stages:
  - build
  - deploy

build-site:
  stage: build
  image: node:latest
  script:
    - npm install pnpm
    - pnpm install
    - touch TESTING_FILE.txt

deploy-site:
  stage: deploy
  script:
    - ls